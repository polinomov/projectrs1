[package]
name = "wasm_demo"
version = "0.1.0"
edition = "2024"

[lib]
#crate-type = ["cdylib", "rlib"]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3

[dependencies]
wasm-bindgen = "0.2"
instant = { version = "0.1", features = ["wasm-bindgen"] }
